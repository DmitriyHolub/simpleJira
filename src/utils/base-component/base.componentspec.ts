import { BaseDirective } from './base.component';

describe('BaseDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseDirective();
    expect(directive).toBeTruthy();
  });
});
